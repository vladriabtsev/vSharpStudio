<UserControl x:Class="vSharpStudio.Views.TreeSubModels"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:vSharpStudio.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
        <ContentControl x:Name="cont" DataContextChanged="grd_DataContextChanged" />
        <!--<ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <StackPanel>
                <Grid x:Name="grd" DataContextChanged="grd_DataContextChanged" ShowGridLines="True" />
            </StackPanel>
        </ScrollViewer>-->
    </Grid>
</UserControl>
